{
	"name": "sde-kit",
	"private": true,
	"version": "0.1.0",
	"type": "module",
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"dev": "npm run dev -w apps/desktop",
		"build": "bash scripts/build.sh",
		"release": "bash scripts/release.sh",
		"check": "npm run check -w apps/desktop",
		"check:all": "npm run check && cargo check --manifest-path apps/desktop/src-tauri/Cargo.toml",
		"test": "cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml && cargo test -p sde-kit-graph",
		"lint": "npm run check -w apps/desktop",
		"clean": "rm -rf apps/*/build apps/*/.svelte-kit apps/*/src-tauri/target"
	}
}
